[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install", "cd client && npm install"]

[phases.build]
cmds = ["cd client && npm run build", "npm prune --omit=dev"]

[start]
cmd = "node src/index.js"
