# Zálohy DB na Cloudflare R2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Automatické denní zálohy `data.db` na Cloudflare R2 s retencí 30 dní, obnovou přes skript a e-mailovým alertem při selhání.

**Architecture:** Izolovaný R2 wrapper (`r2Client.js`) odděluje AWS SDK od logiky zálohování (`backup.js`). Čistá logika (název objektu, výběr ke smazání) je v exportovaných funkcích testovatelných bez sítě; `createBackup` přijímá injektovatelné závislosti (r2 klient, db path, tmp dir, now), takže happy path se testuje s fake klientem. Cron (`scheduler.js`) běží v hlavním procesu a aktivuje se jen při nastavených R2 ENV.

**Tech Stack:** Node.js (CommonJS), better-sqlite3 `.backup()`, `@aws-sdk/client-s3` proti R2, `node-cron`, Brevo (stávající `email.js`), `zlib` (core), testy přes vestavěný `node --test` + `node:assert/strict`.

**Konvence projektu (dodržuj):**
- Testy: soubor `*.test.js` vedle zdroje, `const test = require('node:test')`, `const assert = require('node:assert/strict')`. Spouští se `node --test <soubor>`.
- CommonJS: `require` / `module.exports`. Žádný TypeScript v backendu.
- Žádné secrets v repu — vše přes ENV.
- Po dokončení práce commit + push do větve `staging` (ne main).
- Pre-commit hook (`scripts/bump-version.js`) sám bumpne verzi při každém commitu — je to očekávané, neřeš to.

---

## File Structure

| Soubor | Odpovědnost |
|--------|-------------|
| `src/services/r2Client.js` (create) | Tenký wrapper nad `@aws-sdk/client-s3` pro R2: `put`, `list`, `delete`. Jediné místo, které zná AWS SDK. |
| `src/services/backup.js` (create) | Logika zálohy: `backupObjectKey`, `selectKeysToPrune` (čisté), `createBackup`, `listBackups`, `pruneOldBackups`. |
| `src/services/backup.test.js` (create) | Unit testy čistých funkcí + happy path s fake r2. |
| `src/services/email.js` (modify) | Přidat `sendBackupFailureAlert`. |
| `src/services/scheduler.js` (create) | `startScheduler()` — registruje node-cron jen při nastavených ENV. |
| `src/index.js` (modify) | Zavolat `startScheduler()` v `app.listen` callbacku. |
| `scripts/backup-now.cjs` (create) | Manuální spuštění `createBackup()`. |
| `scripts/restore-backup.cjs` (create) | List / dry-run / ostrá obnova (`CONFIRM=1`). |
| `.env.example` (create/modify) | Dokumentace nových ENV (bez hodnot). |

---

## Task 1: Dependency a ENV scaffolding

**Files:**
- Modify: `package.json` (přidat `@aws-sdk/client-s3`)
- Create/Modify: `.env.example`

- [ ] **Step 1: Nainstaluj AWS SDK v3 S3 klient**

Run: `npm install @aws-sdk/client-s3`
Expected: přidá se do `dependencies` v `package.json`, bez chyb.

- [ ] **Step 2: Zdokumentuj ENV v `.env.example`**

Pokud `.env.example` neexistuje, vytvoř ho. Přidej (existující obsah neměň):

```bash
# --- Zálohy DB na Cloudflare R2 ---
# Bez těchto čtyř se cron záloha NEAKTIVUJE (lokální dev tím pádem nezálohuje).
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
# Volitelné:
BACKUP_ALERT_EMAIL=tomas.strida@gmail.com
BACKUP_CRON=0 3 * * *
BACKUP_RETENTION_DAYS=30
```

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json .env.example
git commit -m "chore(zálohy): @aws-sdk/client-s3 + ENV scaffolding"
```

---

## Task 2: Čisté funkce — `backupObjectKey` a `selectKeysToPrune`

Začínáme čistou, plně testovatelnou logikou bez sítě.

**Files:**
- Create: `src/services/backup.js`
- Create: `src/services/backup.test.js`

- [ ] **Step 1: Napiš padající testy**

V `src/services/backup.test.js`:

```js
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const { backupObjectKey, selectKeysToPrune } = require('./backup');

test('backupObjectKey: formát backups/data-YYYY-MM-DD-HHmmss.db.gz', () => {
  // 2026-06-03 03:00:05 UTC
  const d = new Date(Date.UTC(2026, 5, 3, 3, 0, 5));
  assert.equal(backupObjectKey(d), 'backups/data-2026-06-03-030005.db.gz');
});

test('backupObjectKey: dvojciferné zero-padding', () => {
  const d = new Date(Date.UTC(2026, 0, 9, 7, 8, 9));
  assert.equal(backupObjectKey(d), 'backups/data-2026-01-09-070809.db.gz');
});

test('selectKeysToPrune: vybere jen objekty starší než retenceDays', () => {
  const now = new Date(Date.UTC(2026, 5, 3, 3, 0, 0));
  const day = 24 * 60 * 60 * 1000;
  const objects = [
    { key: 'backups/a', lastModified: new Date(now - 5 * day) },   // 5 dní – ponechat
    { key: 'backups/b', lastModified: new Date(now - 31 * day) },  // 31 dní – smazat
    { key: 'backups/c', lastModified: new Date(now - 40 * day) },  // 40 dní – smazat
  ];
  const toPrune = selectKeysToPrune(objects, now, 30);
  assert.deepEqual(toPrune, ['backups/b', 'backups/c']);
});

test('selectKeysToPrune: přesně na hranici (30 dní) se NEMaže', () => {
  const now = new Date(Date.UTC(2026, 5, 3, 3, 0, 0));
  const day = 24 * 60 * 60 * 1000;
  const objects = [{ key: 'backups/edge', lastModified: new Date(now - 30 * day) }];
  assert.deepEqual(selectKeysToPrune(objects, now, 30), []);
});
```

- [ ] **Step 2: Spusť test, ověř že padá**

Run: `node --test src/services/backup.test.js`
Expected: FAIL — `Cannot find module './backup'` nebo `backupObjectKey is not a function`.

- [ ] **Step 3: Implementuj čisté funkce**

V `src/services/backup.js`:

```js
'use strict';

function pad(n) {
  return String(n).padStart(2, '0');
}

/** Klíč objektu pro zálohu. Datum bereme v UTC kvůli determinismu. */
function backupObjectKey(date) {
  const y = date.getUTCFullYear();
  const mo = pad(date.getUTCMonth() + 1);
  const d = pad(date.getUTCDate());
  const h = pad(date.getUTCHours());
  const mi = pad(date.getUTCMinutes());
  const s = pad(date.getUTCSeconds());
  return `backups/data-${y}-${mo}-${d}-${h}${mi}${s}.db.gz`;
}

/** Vrátí klíče objektů starších než retentionDays vůči `now`. */
function selectKeysToPrune(objects, now, retentionDays) {
  const cutoff = now.getTime() - retentionDays * 24 * 60 * 60 * 1000;
  return objects
    .filter((o) => o.lastModified.getTime() < cutoff)
    .map((o) => o.key);
}

module.exports = { backupObjectKey, selectKeysToPrune };
```

- [ ] **Step 4: Spusť test, ověř že prochází**

Run: `node --test src/services/backup.test.js`
Expected: PASS (4 testy).

- [ ] **Step 5: Commit**

```bash
git add src/services/backup.js src/services/backup.test.js
git commit -m "feat(zálohy): čisté funkce backupObjectKey + selectKeysToPrune"
```

---

## Task 3: R2 klient wrapper

Izolujeme AWS SDK na jedno místo. Bez unit testu (jen tenké přemapování na SDK) — ověří se integračně v Tasku 7.

**Files:**
- Create: `src/services/r2Client.js`

- [ ] **Step 1: Implementuj wrapper**

V `src/services/r2Client.js`:

```js
'use strict';
const {
  S3Client,
  PutObjectCommand,
  ListObjectsV2Command,
  DeleteObjectsCommand,
  GetObjectCommand,
} = require('@aws-sdk/client-s3');

/**
 * Vytvoří R2 klienta s jednoduchým rozhraním { put, list, delete, get, bucket }.
 * Vyhodí chybu, pokud chybí povinné ENV.
 */
function createR2Client(env = process.env) {
  const { R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET } = env;
  if (!R2_ACCOUNT_ID || !R2_ACCESS_KEY_ID || !R2_SECRET_ACCESS_KEY || !R2_BUCKET) {
    throw new Error('Chybí R2 ENV (R2_ACCOUNT_ID/R2_ACCESS_KEY_ID/R2_SECRET_ACCESS_KEY/R2_BUCKET)');
  }

  const client = new S3Client({
    region: 'auto',
    endpoint: `https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com`,
    credentials: { accessKeyId: R2_ACCESS_KEY_ID, secretAccessKey: R2_SECRET_ACCESS_KEY },
  });

  return {
    bucket: R2_BUCKET,

    async put(key, body) {
      await client.send(new PutObjectCommand({ Bucket: R2_BUCKET, Key: key, Body: body }));
    },

    /** Vrací [{ key, lastModified, sizeBytes }] pro daný prefix. */
    async list(prefix) {
      const out = [];
      let token;
      do {
        const res = await client.send(new ListObjectsV2Command({
          Bucket: R2_BUCKET, Prefix: prefix, ContinuationToken: token,
        }));
        for (const o of res.Contents || []) {
          out.push({ key: o.Key, lastModified: o.LastModified, sizeBytes: o.Size });
        }
        token = res.IsTruncated ? res.NextContinuationToken : undefined;
      } while (token);
      return out;
    },

    async delete(keys) {
      if (!keys.length) return;
      await client.send(new DeleteObjectsCommand({
        Bucket: R2_BUCKET,
        Delete: { Objects: keys.map((Key) => ({ Key })) },
      }));
    },

    /** Stáhne objekt jako Buffer. */
    async get(key) {
      const res = await client.send(new GetObjectCommand({ Bucket: R2_BUCKET, Key: key }));
      const chunks = [];
      for await (const chunk of res.Body) chunks.push(chunk);
      return Buffer.concat(chunks);
    },
  };
}

module.exports = { createR2Client };
```

- [ ] **Step 2: Ověř, že se modul načte bez syntaktické chyby**

Run: `node -e "require('./src/services/r2Client'); console.log('ok')"`
Expected: vypíše `ok`.

- [ ] **Step 3: Commit**

```bash
git add src/services/r2Client.js
git commit -m "feat(zálohy): R2 klient wrapper nad @aws-sdk/client-s3"
```

---

## Task 4: `createBackup`, `pruneOldBackups`, `listBackups`

Jádro. `createBackup` přijímá injektovatelné závislosti, takže happy path testujeme s fake r2 (bez sítě). Záloha přes `db.backup()` → gzip → put → prune.

**Files:**
- Modify: `src/services/backup.js`
- Modify: `src/services/backup.test.js`

- [ ] **Step 1: Přidej padající test happy-path s fake r2**

Doplň do `src/services/backup.test.js`:

```js
const zlib = require('node:zlib');
const fs = require('node:fs');
const os = require('node:os');
const path = require('node:path');
const Database = require('better-sqlite3');
const { createBackup } = require('./backup');

function makeTempDb() {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'spendex-bak-'));
  const dbPath = path.join(dir, 'data.db');
  const db = new Database(dbPath);
  db.pragma('journal_mode = WAL');
  db.exec('CREATE TABLE t (id INTEGER PRIMARY KEY, v TEXT)');
  db.prepare('INSERT INTO t (v) VALUES (?)').run('ahoj');
  db.close();
  return { dir, dbPath };
}

function makeFakeR2() {
  const store = new Map();
  return {
    bucket: 'test',
    puts: [],
    deleted: [],
    listResult: [],
    async put(key, body) { this.puts.push({ key, body }); store.set(key, body); },
    async list() { return this.listResult; },
    async delete(keys) { this.deleted.push(...keys); },
    async get(key) { return store.get(key); },
  };
}

test('createBackup: nahraje gzipovaný konzistentní snapshot DB', async () => {
  const { dir, dbPath } = makeTempDb();
  const r2 = makeFakeR2();
  const now = new Date(Date.UTC(2026, 5, 3, 3, 0, 5));

  const res = await createBackup({ r2, dbPath, tmpDir: dir, now });

  assert.equal(res.key, 'backups/data-2026-06-03-030005.db.gz');
  assert.equal(r2.puts.length, 1);

  // Nahraný obsah musí jít rozbalit zpět na validní SQLite s našimi daty.
  const uploaded = r2.puts[0].body;
  const restoredPath = path.join(dir, 'restored.db');
  fs.writeFileSync(restoredPath, zlib.gunzipSync(uploaded));
  const rdb = new Database(restoredPath);
  const row = rdb.prepare('SELECT v FROM t WHERE id = 1').get();
  rdb.close();
  assert.equal(row.v, 'ahoj');

  fs.rmSync(dir, { recursive: true, force: true });
});

test('createBackup: po uploadu smaže staré zálohy přes prune', async () => {
  const { dir, dbPath } = makeTempDb();
  const r2 = makeFakeR2();
  const now = new Date(Date.UTC(2026, 5, 3, 3, 0, 5));
  const day = 24 * 60 * 60 * 1000;
  r2.listResult = [
    { key: 'backups/old', lastModified: new Date(now - 40 * day), sizeBytes: 1 },
    { key: 'backups/fresh', lastModified: new Date(now - 2 * day), sizeBytes: 1 },
  ];

  const res = await createBackup({ r2, dbPath, tmpDir: dir, now, retentionDays: 30 });

  assert.deepEqual(r2.deleted, ['backups/old']);
  assert.equal(res.prunedCount, 1);
  fs.rmSync(dir, { recursive: true, force: true });
});
```

- [ ] **Step 2: Spusť test, ověř že padá**

Run: `node --test src/services/backup.test.js`
Expected: FAIL — `createBackup is not a function`.

- [ ] **Step 3: Implementuj `createBackup`, `pruneOldBackups`, `listBackups`**

Uprav `src/services/backup.js` — ponech existující funkce, přidej nové a rozšiř export:

```js
const fs = require('node:fs');
const path = require('node:path');
const zlib = require('node:zlib');
const Database = require('better-sqlite3');

const DEFAULT_RETENTION_DAYS = Number(process.env.BACKUP_RETENTION_DAYS) || 30;

/** Smaže zálohy starší než retentionDays. Vrací počet smazaných. */
async function pruneOldBackups(r2, now, retentionDays) {
  const objects = await r2.list('backups/');
  const keys = selectKeysToPrune(objects, now, retentionDays);
  await r2.delete(keys);
  return keys.length;
}

/** Vrátí seznam záloh seřazený sestupně dle data (nejnovější první). */
async function listBackups(r2) {
  const objects = await r2.list('backups/');
  return objects.sort((a, b) => b.lastModified.getTime() - a.lastModified.getTime());
}

/**
 * Vytvoří konzistentní snapshot data.db, zkomprimuje a nahraje na R2, pak proruje staré.
 * deps: { r2, dbPath, tmpDir, now, retentionDays }
 */
async function createBackup({
  r2,
  dbPath = process.env.DB_PATH || path.join(__dirname, '../../data.db'),
  tmpDir = require('node:os').tmpdir(),
  now = new Date(),
  retentionDays = DEFAULT_RETENTION_DAYS,
}) {
  const snapshotPath = path.join(tmpDir, `snapshot-${now.getTime()}.db`);
  try {
    // Konzistentní snapshot i ve WAL módu (NE prostá kopie souboru).
    const src = new Database(dbPath, { readonly: true });
    await src.backup(snapshotPath);
    src.close();

    const gz = zlib.gzipSync(fs.readFileSync(snapshotPath));
    const key = backupObjectKey(now);
    await r2.put(key, gz);

    const prunedCount = await pruneOldBackups(r2, now, retentionDays);
    return { key, sizeBytes: gz.length, prunedCount };
  } finally {
    fs.rmSync(snapshotPath, { force: true });
  }
}

module.exports = {
  backupObjectKey,
  selectKeysToPrune,
  pruneOldBackups,
  listBackups,
  createBackup,
};
```

> Pozn.: `db.backup()` v better-sqlite3 vrací Promise — proto `await`. Otevíráme zdroj `readonly`.

- [ ] **Step 4: Spusť celý test soubor, ověř že prochází**

Run: `node --test src/services/backup.test.js`
Expected: PASS (6 testů celkem).

- [ ] **Step 5: Commit**

```bash
git add src/services/backup.js src/services/backup.test.js
git commit -m "feat(zálohy): createBackup + prune + listBackups s testy"
```

---

## Task 5: E-mail alert při selhání

**Files:**
- Modify: `src/services/email.js`

- [ ] **Step 1: Přidej `sendBackupFailureAlert`**

V `src/services/email.js` před `module.exports` přidej:

```js
async function sendBackupFailureAlert(err) {
  const to = process.env.BACKUP_ALERT_EMAIL || 'tomas.strida@gmail.com';
  const when = new Date().toISOString();
  await sendEmail({
    to,
    subject: '⚠️ Spendex: záloha DB selhala',
    htmlContent: `
      <p>Denní záloha databáze Spendex selhala.</p>
      <p><strong>Čas:</strong> ${when}</p>
      <p><strong>Chyba:</strong> ${err && err.message ? err.message : String(err)}</p>
      <pre>${err && err.stack ? err.stack : ''}</pre>
    `,
  });
}
```

- [ ] **Step 2: Rozšiř export**

Změň poslední řádek na:

```js
module.exports = { sendEmail, sendVerificationEmail, sendPasswordResetEmail, sendBackupFailureAlert };
```

- [ ] **Step 3: Ověř načtení modulu**

Run: `node -e "const e=require('./src/services/email'); console.log(typeof e.sendBackupFailureAlert)"`
Expected: vypíše `function`.

- [ ] **Step 4: Commit**

```bash
git add src/services/email.js
git commit -m "feat(zálohy): e-mail alert sendBackupFailureAlert"
```

---

## Task 6: Scheduler + napojení v `index.js`

**Files:**
- Create: `src/services/scheduler.js`
- Create: `src/services/scheduler.test.js`
- Modify: `src/index.js`

- [ ] **Step 1: Napiš padající test (bez ENV se cron nezaregistruje)**

V `src/services/scheduler.test.js`:

```js
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const { shouldSchedule } = require('./scheduler');

test('shouldSchedule: false když chybí povinné R2 ENV', () => {
  assert.equal(shouldSchedule({}), false);
  assert.equal(shouldSchedule({ R2_ACCOUNT_ID: 'a' }), false);
});

test('shouldSchedule: true když jsou všechny povinné R2 ENV', () => {
  assert.equal(shouldSchedule({
    R2_ACCOUNT_ID: 'a', R2_ACCESS_KEY_ID: 'b', R2_SECRET_ACCESS_KEY: 'c', R2_BUCKET: 'd',
  }), true);
});
```

- [ ] **Step 2: Spusť test, ověř že padá**

Run: `node --test src/services/scheduler.test.js`
Expected: FAIL — `shouldSchedule is not a function`.

- [ ] **Step 3: Implementuj scheduler**

V `src/services/scheduler.js`:

```js
'use strict';
const cron = require('node-cron');
const { createBackup } = require('./backup');
const { createR2Client } = require('./r2Client');
const { sendBackupFailureAlert } = require('./email');

function shouldSchedule(env = process.env) {
  return Boolean(
    env.R2_ACCOUNT_ID && env.R2_ACCESS_KEY_ID && env.R2_SECRET_ACCESS_KEY && env.R2_BUCKET
  );
}

async function runBackupJob() {
  try {
    const r2 = createR2Client();
    const res = await createBackup({ r2 });
    console.log(`[backup] OK ${res.key} (${res.sizeBytes} B, prune ${res.prunedCount})`);
  } catch (err) {
    console.error('[backup] SELHALO:', err);
    try {
      await sendBackupFailureAlert(err);
    } catch (alertErr) {
      console.error('[backup] alert e-mail selhal:', alertErr);
    }
  }
}

function startScheduler() {
  if (!shouldSchedule()) {
    console.warn('[backup] R2 ENV nenastaveno — cron záloha NEAKTIVNÍ');
    return;
  }
  const expr = process.env.BACKUP_CRON || '0 3 * * *';
  cron.schedule(expr, runBackupJob, { timezone: 'Europe/Prague' });
  console.log(`[backup] cron aktivní: "${expr}" (Europe/Prague)`);
}

module.exports = { shouldSchedule, startScheduler, runBackupJob };
```

- [ ] **Step 4: Spusť test, ověř že prochází**

Run: `node --test src/services/scheduler.test.js`
Expected: PASS (2 testy).

- [ ] **Step 5: Napoj scheduler v `src/index.js`**

V `src/index.js` uprav `app.listen` callback (cca ř. 70):

```js
app.listen(PORT, () => {
  console.log(`Spendex running on port ${PORT}`);
  require('./services/scheduler').startScheduler();
});
```

- [ ] **Step 6: Ověř, že app nastartuje lokálně bez R2 ENV (cron se jen přeskočí)**

Run: `node -e "require('./src/services/scheduler').startScheduler()"`
Expected: vypíše `[backup] R2 ENV nenastaveno — cron záloha NEAKTIVNÍ`, proces skončí bez chyby.

- [ ] **Step 7: Commit**

```bash
git add src/services/scheduler.js src/services/scheduler.test.js src/index.js
git commit -m "feat(zálohy): node-cron scheduler napojený v index.js"
```

---

## Task 7: Manuální trigger `backup-now.cjs`

**Files:**
- Create: `scripts/backup-now.cjs`

- [ ] **Step 1: Implementuj skript**

V `scripts/backup-now.cjs`:

```js
#!/usr/bin/env node
'use strict';
require('dotenv').config();
const { createBackup } = require('../src/services/backup');
const { createR2Client } = require('../src/services/r2Client');

(async () => {
  try {
    const r2 = createR2Client();
    console.log('Spouštím zálohu…');
    const res = await createBackup({ r2 });
    console.log(`Hotovo: ${res.key} (${res.sizeBytes} B), smazáno starých: ${res.prunedCount}`);
    process.exit(0);
  } catch (err) {
    console.error('Záloha selhala:', err);
    process.exit(1);
  }
})();
```

- [ ] **Step 2: Ověř chování bez ENV (musí čistě zhlásit chybějící ENV)**

Run: `node scripts/backup-now.cjs`
Expected: vypíše `Záloha selhala: Error: Chybí R2 ENV ...`, exit 1. (S reálnými ENV proběhne ostrá záloha — to ověříš až po nastavení R2.)

- [ ] **Step 3: Commit**

```bash
git add scripts/backup-now.cjs
git commit -m "feat(zálohy): skript backup-now pro manuální zálohu"
```

---

## Task 8: Obnova `restore-backup.cjs`

Destruktivní operace — dry-run je default, ostrá jen pod `CONFIRM=1`, předtím bezpečnostní kopie stávající DB.

**Files:**
- Create: `scripts/restore-backup.cjs`

- [ ] **Step 1: Implementuj skript**

V `scripts/restore-backup.cjs`:

```js
#!/usr/bin/env node
'use strict';
require('dotenv').config();
const fs = require('node:fs');
const path = require('node:path');
const zlib = require('node:zlib');
const Database = require('better-sqlite3');
const { listBackups } = require('../src/services/backup');
const { createR2Client } = require('../src/services/r2Client');

const DB_PATH = process.env.DB_PATH || path.join(__dirname, '../data.db');

function ts() {
  return new Date().toISOString().replace(/[:.]/g, '-');
}

(async () => {
  const key = process.argv[2];
  const confirm = process.env.CONFIRM === '1';
  const r2 = createR2Client();

  // Bez klíče → vypiš dostupné zálohy.
  if (!key) {
    const list = await listBackups(r2);
    if (!list.length) {
      console.log('Žádné zálohy v R2.');
      return;
    }
    console.log('Dostupné zálohy (nejnovější první):');
    for (const b of list) {
      console.log(`  ${b.key}\t${b.lastModified.toISOString()}\t${b.sizeBytes} B`);
    }
    console.log('\nObnova: node scripts/restore-backup.cjs <klíč>            (dry-run)');
    console.log('Ostrá:  CONFIRM=1 node scripts/restore-backup.cjs <klíč>');
    return;
  }

  // Stáhni a rozbal do temp.
  console.log(`Stahuji ${key} …`);
  const gz = await r2.get(key);
  const raw = zlib.gunzipSync(gz);
  const tmpPath = path.join(require('node:os').tmpdir(), `restore-${ts()}.db`);
  fs.writeFileSync(tmpPath, raw);

  // Ověř integritu + spočítej řádky pro přehled.
  const rdb = new Database(tmpPath, { readonly: true });
  const tables = rdb.prepare(
    "SELECT name FROM sqlite_master WHERE type='table' ORDER BY name"
  ).all().map((r) => r.name);
  const txCount = tables.includes('transactions')
    ? rdb.prepare('SELECT COUNT(*) c FROM transactions').get().c
    : null;
  rdb.close();

  if (!confirm) {
    console.log('\n=== DRY-RUN (nic se nepřepíše) ===');
    console.log(`Zdroj:        ${key}`);
    console.log(`Velikost:     ${raw.length} B (rozbaleno)`);
    console.log(`Tabulek:      ${tables.length} (${tables.join(', ')})`);
    if (txCount !== null) console.log(`Transakcí:    ${txCount}`);
    console.log(`Cíl (DB_PATH): ${DB_PATH}`);
    console.log('\nPro ostrou obnovu spusť znovu s CONFIRM=1.');
    fs.rmSync(tmpPath, { force: true });
    return;
  }

  // Ostrá obnova: bezpečnostní kopie stávající DB, pak přepiš.
  if (fs.existsSync(DB_PATH)) {
    const backupPath = `${DB_PATH}.before-restore-${ts()}`;
    fs.copyFileSync(DB_PATH, backupPath);
    console.log(`Bezpečnostní kopie stávající DB: ${backupPath}`);
  }
  fs.copyFileSync(tmpPath, DB_PATH);
  fs.rmSync(tmpPath, { force: true });
  console.log(`Obnoveno z ${key} do ${DB_PATH}.`);
  console.log('Restartuj aplikaci, aby se znovu otevřela DB.');
})().catch((err) => {
  console.error('Obnova selhala:', err);
  process.exit(1);
});
```

- [ ] **Step 2: Ověř chování bez ENV**

Run: `node scripts/restore-backup.cjs`
Expected: vypíše `Obnova selhala: Error: Chybí R2 ENV ...`, exit 1. (List/dry-run/ostrá se ověří až s reálnými R2 ENV.)

- [ ] **Step 3: Commit**

```bash
git add scripts/restore-backup.cjs
git commit -m "feat(zálohy): restore skript (dry-run default + CONFIRM=1)"
```

---

## Task 9: Závěrečné ověření a push

- [ ] **Step 1: Spusť všechny testy služby zálohování**

Run: `node --test src/services/backup.test.js src/services/scheduler.test.js`
Expected: PASS, žádný fail.

- [ ] **Step 2: Spusť celou testovou sadu projektu (regrese)**

Run: `node --test $(find src scripts -name "*.test.js")`
Expected: vše PASS — nic jsme nerozbili.

- [ ] **Step 3: Push do staging**

```bash
git push origin staging
```

Expected: Railway nasadí staging. (Cron se aktivuje až po nastavení R2 ENV na Railway.)

---

## Manuální kroky uživatele (mimo kód, po nasazení)

Tyto kroky udělá uživatel (Claude jen provede instrukcemi):

1. **Cloudflare R2:** vytvořit privátní bucket (např. `spendex-backups`).
2. **API token:** vygenerovat R2 token se scope na tento bucket, oprávnění **Object Read & Write**.
3. **Railway ENV:** nastavit `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET` (volitelně `BACKUP_ALERT_EMAIL`).
4. **Ověření:** přes Railway shell spustit `node scripts/backup-now.cjs` a zkontrolovat, že objekt přibyl v bucketu.
5. **Restore drill:** `node scripts/restore-backup.cjs` (list) → `node scripts/restore-backup.cjs <klíč>` (dry-run), ověřit počty.

---

## Self-Review (proti specu)

- **Spec coverage:** R2 destinace (Task 3), denně 03:00 + retence 30 (Task 6 cron + Task 2/4 prune), jen data.db (Task 4 dbPath), bez šifrování (nikde se nešifruje), cron v appce (Task 6), restore dry-run + CONFIRM (Task 8), alert e-mail (Task 5), ENV (Task 1), testy (Task 2/4/6), R2 token least-privilege (manuální kroky). ✓ Vše pokryto.
- **Placeholders:** žádné TBD/TODO; každý krok má reálný kód a očekávaný výstup. ✓
- **Type/název konzistence:** `createR2Client` → `{ put, list, delete, get, bucket }` použité shodně v backup.js, scheduler.js, obou skriptech; `createBackup({ r2, dbPath, tmpDir, now, retentionDays })` shodné v testu i volajících; `selectKeysToPrune(objects, now, retentionDays)` shodné. ✓
